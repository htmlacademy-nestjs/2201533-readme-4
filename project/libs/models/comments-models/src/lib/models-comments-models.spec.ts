import { modelsCommentsModels } from './models-comments-models';

describe('modelsCommentsModels', () => {
  it('should work', () => {
    expect(modelsCommentsModels()).toEqual('models-comments-models');
  });
});
